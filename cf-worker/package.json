{
  "name": "tinsleytoolbox-avatar-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Password-gated Cloudflare Worker that proxies avatar stylization requests from the GitHub Pages frontend to OpenAI Images.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.78.0"
  }
}
